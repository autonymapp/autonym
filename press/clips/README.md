# Demo clips checklist

Same deal as the screenshots — I can't record these, but here's what's worth capturing. Keep the
raw, uncompressed captures here; compress a delivery copy separately if a platform needs it.

- Fandom wiki instant import — paste a character wiki URL into "Fill from Wiki/Text" and watch the
  sheet populate.
- Waypoint drag-and-drop reorder in Journeys.
- A Beacon generating unattended — a few turns streaming in back-to-back with no input.
- (Optional) The Mood & Pacing selector changing a reply's tone on regenerate — same prompt, two
  different presets, side by side.
